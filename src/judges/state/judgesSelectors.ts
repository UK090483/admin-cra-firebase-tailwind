import { createSelector } from "reselect";
import { RootState } from "redux/Reducers/RootReducer";

// export const selectAllJudges = createSelector(
//   (state: RootState) => state.judges.data,
//   (data) => data
// );

// export const selectJudgeById = createSelector(
//   (state: RootState, id: string) =>
//     state.judges.data[id] ? state.judges.data[id] : null,
//   (data) => data
// );

export {};
