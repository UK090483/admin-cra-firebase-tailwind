import { IApplicationRecord } from "applications/ApplicationTypes";

// const getAssessment = (application: IApplicationRecord) => {
//   return application && application.assessments
//     ? Object.values(application.assessments)[0]
//     : undefined;
// };

export {};
